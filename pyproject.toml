[project]
name = "hermes-kev-router"
version = "0.1.1"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "BLE"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["N999"]
